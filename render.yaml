services:
  - type: web
    name: happydent-clinic
    runtime: java
    rootDir: backend
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/*.jar
    plan: free
    envVars:
      - key: PORT
        value: 8080
